#include <string.h>
#include "main.h"

/**
 *_strlen - A function that returns the length of a string.
 *@s: String at address
 *
 *Return: len (length of string)
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
