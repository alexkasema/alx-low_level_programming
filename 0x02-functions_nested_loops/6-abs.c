#include <stdlib.h>
#include <ctype.h>
#include <math.h>
#include "main.h"

/**
 *_abs - A function that computes the absolute value of an integer.
 *@n: Integer input
 *
 *Return: absolut(positive number)
 */
int _abs(int n)
{
	int absolut = abs(n);
	return (absolut);
}
