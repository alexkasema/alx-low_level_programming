#include "main.h"

/**
 *add - A function that adds two integers and returns the result.
 *@a: User input
 *@b: user Input
 *
 *Return: result
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
